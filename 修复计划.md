# MiniOB 功能修复计划

## 当前状态

### ✅ 已修复
1. **NULL/NOT NULL 语法解析** - 修复了 `nullable` 规则的默认值问题
2. **浮点数精度** - 修复了 `FloatType::to_string` 的精度问题
3. **唯一索引重复键检查** - 修复了唯一索引的重复键检查逻辑

### ❌ 待修复（按优先级）

#### 高优先级（影响基础功能）
1. **SQL解析失败**
   - `create-view`: `create table create_view_t2(id int not null, age int, name char(10));` - SQL_SYNTAX
   - `vector-basic`: `INSERT INTO TEST VALUES(1, STRING_TO_VECTOR('[5.86,1.69,0.67]'));` - SQL_SYNTAX
   - `vector-search`: `INSERT INTO TAB_VEC VALUES(1, 1, STRING_TO_VECTOR(...));` - SQL_SYNTAX
   - `full-text-index`: `SELECT TOKENIZE(...)` - SQL_SYNTAX

2. **INSERT/UPDATE失败**
   - `Alter`: `INSERT INTO alter_table_1 VALUES (6);` - FAILURE
   - `Date`: `INSERT INTO date_table VALUES (1,'2020-01-21');` - FAILURE（创建索引后）
   - `Update`: `INSERT INTO update_table_1 VALUES (1, 'FYUIGRO6IHU98WC7R', 16, 46);` - FAILURE
   - `update-mvcc`: `insert into t_update_mvcc values(1, 1, 'a');` - FAILURE

3. **唯一索引创建失败**
   - `Unique`: `CREATE UNIQUE INDEX index_id on unique_table1(id);` - FAILURE

4. **内存错误和崩溃**
   - `complex-sub-query`: 崩溃 - `RowRecordPageHandler::get_record`
   - `drop-table`: `free(): invalid pointer`
   - `multi-index`: `free(): invalid pointer`

#### 中优先级（功能实现）
5. **function功能** - LENGTH, ROUND, DATE_FORMAT
6. **union功能** - UNION 和 UNION ALL 去重
7. **join-tables功能** - INNER JOIN
8. **vector-basic功能** - 向量类型和函数
9. **vector-search功能** - 向量搜索
10. **alter功能** - ALTER TABLE
11. **complex-sub-query功能** - 关联子查询
12. **create-view功能** - 视图
13. **full-text-index功能** - 全文索引

#### 低优先级（扩展功能）
14. **big-order-by功能** - 外部排序

## 修复策略

### 第一阶段：修复解析和基础功能
1. 修复 SQL 解析问题（NULL/NOT NULL, TOKENIZE, STRING_TO_VECTOR）
2. 修复 INSERT/UPDATE 失败问题
3. 修复唯一索引创建问题
4. 修复日期类型索引插入问题

### 第二阶段：实现核心功能
1. 实现 function 功能
2. 实现 union 功能
3. 实现 join-tables 功能
4. 实现 vector-basic 功能

### 第三阶段：实现高级功能
1. 实现 alter 功能
2. 实现 complex-sub-query 功能
3. 实现 create-view 功能
4. 实现 full-text-index 功能

### 第四阶段：修复内存问题和优化
1. 修复内存错误和崩溃问题
2. 实现 big-order-by 功能（如果需要）

## 下一步行动

1. 先修复 SQL 解析问题，确保所有 SQL 语句都能正确解析
2. 修复 INSERT/UPDATE 失败问题，确保数据能正确插入和更新
3. 修复唯一索引创建问题，确保唯一索引能正确创建和使用
4. 逐步实现其他功能

